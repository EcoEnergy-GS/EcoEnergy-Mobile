<!-- res/drawable/banner_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sombra -->
    <item android:bottom="6dp" android:right="6dp">
        <shape android:shape="rectangle">
            <corners android:radius="20dp" />
            <solid android:color="#B3B3B3" /> <!-- Cor da sombra -->
        </shape>
    </item>
    <!-- Fundo do banner -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp" />
            <solid android:color="@color/grass_green" /> <!-- Cor do banner -->
        </shape>
    </item>
</layer-list>
